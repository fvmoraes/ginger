# Fase 0 (GIN-025/D-06): lint mínimo para CI verde.
# errcheck (9 issues) será habilitado na Fase 2 junto com a limpeza de erros.
version: "2"

linters:
  disable:
    - errcheck

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
